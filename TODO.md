# TODO — tomorrow

- [x] Review & commit the bcrypt password-hashing change (`backend/models/User.js`, `backend/controllers/authController.js`) — already committed on `main` in `53e790b`.
- [x] Manually test register + login flows against a real/local DB to confirm hashing + legacy-plaintext fallback both work. Verified live against local MongoDB: register → password stored as `$2b$...` bcrypt hash; login with correct/wrong password both behave correctly; a row forced to legacy plaintext still authenticates via the fallback and is transparently rehashed to bcrypt on success. Test user cleaned up afterward.
- [x] Decide if existing plaintext-password users in the DB need a one-time migration, or if the transparent rehash-on-login fallback in `comparePassword` is enough. Decision: no migration script needed — the fallback rehashes each account the next time it logs in, verified above, and it's zero-downtime. Revisit only if there are accounts that may never log in again and need to be forced off plaintext sooner.
- [x] Double check `backend/.env` is not staged/committed — confirmed not tracked (`git ls-files` has no match) and covered by `.gitignore` (`backend/.gitignore:2` and root `.gitignore`).
- [x] Sweep other places that create `User` docs directly (e.g. any seed/admin scripts) to make sure none bypass the model's `pre('save')` hook in a way that skips hashing. No seed/admin scripts exist. Only `new User().save()` in `authController.js` creates users (goes through the hook); `adminController.js` only uses `findByIdAndUpdate` on `isApproved`, never touching `password`.
